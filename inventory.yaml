all:
  hosts:
    localhost:
      ansible_connection: local
  vars:
    ntp_server: au.pool.ntp.org
    ntp_timezone: Australia/Sydney
    ntp_servers:
      - 0.au.pool.ntp.org
      - 1.au.pool.ntp.org
      - 2.au.pool.ntp.org
      - 3.au.pool.ntp.org
